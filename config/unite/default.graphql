
type Article implements UniteContent {
    id: ID
    _deleted: DateTime
    _version: Int!              @hide(if: "not is_granted('ROLE_ADMIN')")
    _versions: [Article!]!

    title: String!              @field(type: "text") @hide(if: "true")
    link: UniteLink!            @field(type: "link")
}
