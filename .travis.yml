language: php

# because we need to install mysql 5.7 first
sudo: required
dist: trusty
os: linux

matrix:
    include:
        #- php: 7.1
        #- php: 7.2
        #- php: 7.2
        #  env: ROUTING_APPROACH=parameter
        - php: 7.2
          env: coverage=true

    fast_finish: true

cache:
    directories:
        - $HOME/.composer/cache/files

services:
  - mysql

before_install:
  - composer self-update
  - mysql -e 'CREATE DATABASE unite_test;'
  - cp .env.dist .env
  - curl -O https://phar.phpunit.de/phpunit-7.1.0.phar

install:
  - composer install

before_script:
  - bash .travis.install-mysql-5.7.sh
  - |
    if [[ $coverage ]]; then
      phpenv config-rm xdebug.ini
      curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
      chmod +x ./cc-test-reporter
      ./cc-test-reporter before-build
    fi

script:
  - |
    if [[ $coverage ]]; then
      ulimit -n 10000
      phpdbg -qrr phpunit-7.1.0.phar --coverage-clover clover.xml
    else
      phpunit
    fi

after_success:
  - |
    if [[ $coverage ]]; then
      ./cc-test-reporter after-build -t clover --exit-code $TRAVIS_TEST_RESULT
    fi