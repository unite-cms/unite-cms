{% extends '@UnitedCMSCore/layout.html.twig' %}
{% block title %}{{ domain }} / {{ settingType }} / Revert revision #{{ setting.id }}{% endblock title %}
{% block navigation %}{{ include('@UnitedCMSCore/Domain/_navigation.html.twig') }}{% endblock navigation %}
{% block content %}
<article class="uk-card uk-card-default uk-card-body uk-width-1-1 full-content-card">
    <header class="uk-card-header uk-flex">
        <a class="back-button" href="{{ path('unitedcms_core_setting_revisions', {
            domain: united.domain.identifier,
            organization: united.organization.identifier,
            setting_type: settingType.identifier,
            setting: setting.id
        }) }}"><i data-feather="arrow-left"></i></a>
        <h2 class="uk-flex-1">{% trans with { "%type%": settingType, "%version%": version } %}setting.revisions.revert.headline{% endtrans %}</h2>
    </header>
    {{ form(form) }}
</article>
{% endblock content %}