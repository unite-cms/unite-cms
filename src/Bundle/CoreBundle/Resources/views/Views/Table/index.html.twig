<article class="uk-card uk-card-default uk-card-body uk-width-1-1 full-content-card">
    <header class="uk-card-header uk-flex">
        <h2 class="uk-flex-1">
            {{ view.contentType }}
            {% if view.identifier != 'all' %} » {{ view }}{% endif %}
        </h2>
        {% if parameters.selectModeNone %}
            <a href="{{ parameters.url('create') }}" class="uk-button uk-button-primary">
                <i data-feather="plus" class="icon-left"></i>
                {% trans %}content.create.button{% endtrans %}
            </a>
        {% endif %}
    </header>

    <united-cms-core-view-table parameters='{{ parameters|json_encode|raw }}'></united-cms-core-view-table>
</article>
