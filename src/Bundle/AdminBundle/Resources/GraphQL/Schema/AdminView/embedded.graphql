
directive @embeddedAdminView on FRAGMENT_DEFINITION

type EmbeddedAdminView implements UniteAdminView {
    id: ID!
    type: ID!
    name: String!
    fragment: String!
    category: String!
    fields: [UniteAdminViewField!]!
    permissions: UniteContentListPermissions!
}
