
directive @settingsAdminView(name: String if: Expression) on FRAGMENT_DEFINITION

type SettingsAdminView implements UniteAdminView{
    id: ID!
    type: ID!
    name: String!
    fragment: String!
    category: String!
    fields: [UniteAdminViewField!]!
}
